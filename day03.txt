day03
실행(alt+o+L), (>!)
0.복습

리스트 태그
<ol> </ol> : 순서 있는 리스트를 만든다
<ul> </ul> : 순서 없는 리스트를 만든다
<li></li> : ol또는 ul 태그 내부에서 사용한다.


리스트 태그 구조
<ol>
	<li></li>
	<li></li>
	....

</ol> 

ol 태그의 옵션
1) type 옵션
	"A" : 알파벳 a부터 순서 지정(대소문자 구분됨)
	"I" : 로마 숫자 
	"1" : 숫자로 지정(기본값)

2) start 옵션
	시작값을 지정할 수 있다.

테이블 태그(표 만들기)
<table></table>
<caption></captioin>
<tr></tr> : 행
<td></td> : 열
	colspan, rowspan 속성을 사용
<th></th> : 제목행에 컬럼을 만드는 태그

이미지 삽입
<img src =  "파일 경로" alt = "설명">

링크를 거는 a태그(앵커 태크)
<a href = "www.naver.com" >네이버</a>

인라인/ 블록 요소
<span></span> : 대표적인 인라인 태그
<div></dive> : 대표적인 블록 태그
	-span과 div 태그는 사이트 화면의 레이아웃을 나누는데 주로
	사용되며 매우 중요하다
	- 둘다 의미가 없다

인라인 요소와 블록 요소

HTML의 모든 요소는 인라인과 블록으로 나뉜다

인라인 요소
	-내용물의 크기만큼만 영역을 차지하므로 다른 요소와 같은 줄에 
	위치할 수 있다. 즉 줄바꿈이 되지 않음
	-span, img, strong, a

블록요소
	-내용물과 상관없이 한 줄을 다 차지하므로 다른 욧와 같은 줄에 위치할 수 없다
	너비 속성이 100%를 다 차지하고 자도응로 줄바꿈 된다
	-다른 요소를 하나의 블록으로 묶어주기 위해 사용한다		
	-블록요소는 직사각형 모양이다
	- h1, div, p(줄바꿈)

css(Cascading Style Sheets)
HTML로 만든 웹문서의 스타일(디자인)을 담당한다.
스타일이란 글꼴, 색상, 테두리, 정렬, 배치(레이아웃)등 외관을 말한다
HTML에서 제공하는 속성으로도 간단한 스타일 지정할 수 있다.
그러나 내용과 디자인이 분리되어 있어야 유지보수에 유리하므로 css를 사용하는 것이 더 좋다.

css 형식 
속성1: 속성값;
속성2: 속성값;
속성은 미리 정해져 있으며 필요에 따라 검색하여 사용한다.

스타일 적용하는 방법

1.인라인 스타일
	스타일을 적용시킬 태그에 style속성을 사용한다.
	
2.내부 스타일
	<head>영역에 <style></style> 태그를 사용하여 각 요소들의
	스타일을 정의한다
	형식
	
	선택자{
		속성1 : 속성값1;
		속성2 : 속성값2;	
	}
	선택자 : 어떤 요소를 선택하기 위해 사용하는 것
id와 class
id와 class속성은 태그들을 식별하고 그룹화 하는데 사용한다
모든 태그에 사용 가능한 속성이다.

id
특정 태그에 식별 가능한 고유한 값을 부여한다
선택자로 사용할 때는 #id로 사용한다

class 
여러 태그를 하나의 집단으로 묶어줌
선택자로 사용할 때는 .class로 사용한다

하나의 태그에 id와 class를 동시에 가질 수 있으며  class를 여러개 가질 수도 있다
주로 class를 많이 사용하며 id는 그 페이지에서 중요하고 유일한 요소에게 부여하는 것이 좋다

3. 외부 스타일
	별도의 css파일을 만들어 적용시킨다.	
	css와 html을 완전히 분리하여 관리할 수 있기 때문에 가장 많이 사용하는 방식이다
	css파일을 html파일에 적용시키기 위해서는 head 영역에 link태그를 사용한다
	<link rel = "stylesheet" href="파일경로">
	rel 속성은 현재 페이지와의 관계를 의미하여, href는 적용시킬 css파일의 경로를 적어준다